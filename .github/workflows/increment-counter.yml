name: Increment Monthly Counter

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  increment-counter:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run Counter Increment
        run: node scripts/increment-counter.js

      - name: Log Result
        run: echo "Counter increment job completed at $(date)"
