# 2026-07-01 — Daily Notes

## Website Agent Session (Slack #websuite)

### Work Done Today (11 commits to main)

1. **Cosmos Address Generator** (`93b7186`)
   - Rewrote with proper crypto: @scure/bip32, @scure/bip39, @noble/curves, @noble/hashes
   - Real BIP32/BIP44 derivation, secp256k1, RIPEMD160, Bech32 encoding
   - 10 Cosmos chains (ATOM, OSMO, JUNO, SCRT, STARS, AKT, KAVA, INJ, CRE, UMEE)
   - Fixed pre-existing AddressChecksumView build error (ethereum-cryptography → @noble/hashes/sha3)

2. **Cosmos SEO** (`d446296`)
   - Title, meta description, keywords with all 10 chain names + tickers
   - Per-chain H3 sections for crawlable content

3. **Canonical URL Fix** (`9f7af5c`)
   - index.html canonical was pointing to `formatho.com/tools/` instead of `formatho.com/`
   - inject-tool-meta.js had `baseUrl = formatho.com/tools` causing double `/tools/tools/` canonicals
   - Organization + WebSite JSON-LD urls fixed
   - SearchAction template fixed

4. **nginx SSG Fix** (`bfc297d`)
   - `try_files` was missing `$uri.html` — pre-rendered SSG pages never served
   - Every request fell back to root index.html → all pages had homepage title/canonical
   - Root cause of both GSC "Excluded by noindex" and "Alternative page with proper canonical" issues

5. **RWA Tokenization Lab** (`ecee840` → `b54be2b`, 7 commits)
   - MetaMask/Rabby wallet connection with auto-detection
   - Chain ID, name, balance display (eth_getBalance)
   - ERC-20 Mirror Token deployment (viem ABI encoding, eth_sendTransaction)
   - Custom Bytecode deployment tab
   - Removed: Price Oracle Mock, Real Estate NFT template, Permission List template (per Nitin's requests)
   - TokenizationFactory section with Solidity source
   - Full RWA architecture: 9 contracts (AssetRegistry, AssetNFT, FractionalToken, Vault, ComplianceManager, TransferManager, IdentityRegistry, Treasury, Governance+Timelock)
   - ASCII architecture diagram
   - Comprehensive SEO: FAQ, use cases, factory pattern

6. **RWA Asset Swap** (`154af9a`)
   - LI.FI integration for cross-chain RWA token swaps
   - API key kept server-side via nginx proxy (`/api/lifi/` → `li.quest/v1/` with X-Api-Key header)
   - Dockerfile envsubst template for key injection
   - 15+ EVM chains supported
   - Token filtering: RWA (OUSG, USDY, STBT, BUIDL), stablecoins (USDC, USDT, DAI, FRAX, LUSD, USDe, PYUSD), liquid staking (wstETH, cbETH, rETH, WBTC), native gas tokens
   - Quote engine with debounced fetching
   - Auto ERC-20 approval check
   - Cross-chain status polling
   - LI.FI API key: in .env on droplet (gitignored)

7. **AOS_SIGN_OFF_REPORT.md** recreated
   - Full audit: 119 routes, 90 tools, 134 views, 66 blog posts
   - 385 commits since previous March 13 report
   - All previous fixes stable

### Key Technical Details

- **LI.FI API key** stored in `.env` on DigitalOcean droplet. nginx proxy injects it server-side. Key value in Slack message from Nitin.
- **Nitin Gurbani** is the human behind Formatho (Slack user U0AK87PPDCH, also U0AJYP6GGEQ)
- **GSC issues**: canonical URL problems now fixed, need Google recrawl
- **Build warnings**: 213 `document is not defined` SSG errors (pre-existing, non-blocking)
- **Dependabot**: 176 vulnerabilities flagged (need npm audit fix)

### People
- **Nitin Gurbani** (@U0AK87PPDCH / @U0AJYP6GGEQ) — Formatho owner, directs website work
- Communicates via Slack #websuite channel (C0AJYVA1KHA)
